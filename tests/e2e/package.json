{
  "name": "keds-e2e",
  "private": true,
  "description": "Read-only smoke tests run against Upsun PR preview environments (see .github/workflows/pr-e2e.yml).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
