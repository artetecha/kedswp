{
  "name": "keds-crm-e2e",
  "private": true,
  "description": "Mutating end-to-end test of the FluentCRM <-> LearnPress enrollment/completion workflow, run against a designated Upsun test environment (see .github/workflows/crm-workflow-e2e.yml). NOT part of the read-only per-PR suite.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
